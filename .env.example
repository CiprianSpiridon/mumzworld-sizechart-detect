# Application Settings
APP_NAME=sizechart-detection
DOCKER_WORKDIR=/usr/src/app
DATA_DIR=data

# AWS Credentials
AWS_ACCESS_KEY_ID=AKIA1234567890EXAMPLE
AWS_SECRET_ACCESS_KEY=abcdef1234567890abcdef1234567890example
AWS_REGION=us-east-1

# Infrastructure Settings
VPC_CIDR=10.0.0.0/16
SUBNET_CIDR=10.0.1.0/24
INSTANCE_TYPE=t3.medium
VOLUME_SIZE=30
VOLUME_TYPE=gp3
INSTANCE_USERNAME=ec2-user

# Security Settings
SSH_INGRESS_CIDR=0.0.0.0/0  # Restrict this to your IP or VPN range in production
SSH_PORT=22

# AMI Settings
AMI_PATTERN=al2023-ami-*-x86_64
AMI_OWNER=amazon

# SSH Key Settings
SSH_KEY_TYPE=rsa
SSH_KEY_BITS=2048

# Terraform Settings
TF_VERSION=1.0.0

# Docker Settings
DOCKER_LATEST_TAG=latest

# Note: This is an example file. In GitHub:
# 1. Go to your repository's Settings
# 2. Navigate to Secrets and Variables > Actions
# 3. Add each of these values as a secret
# 4. Never commit actual secret values to version control 